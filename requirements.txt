# requirements.txt — InvestAI (Streamlit)
# ==========================================
# Instalado automáticamente por Streamlit Community Cloud a partir de
# este archivo. Se usan versiones EXACTAS (==) en vez de rangos (>=) para
# que el resolver de pip no tenga que probar múltiples combinaciones.
#
# IMPORTANTE: en el dashboard de tu app → Settings → Advanced settings →
# "Python version", debe estar fijado en **Python 3.12** (ya confirmado
# que funciona en el último log). No cambiar sin re-verificar wheels.
#
# tensorflow-cpu 2.21.0 requiere protobuf>=6.31, pero streamlit 1.38.0
# requiere protobuf<6 — son incompatibles entre sí. tensorflow-cpu 2.17.0
# requiere protobuf>=3.20.3,<5.0.0, que SÍ se solapa con el rango de
# streamlit. Verificado que 2.17.0 tiene wheel cp312 (Python 3.12).

streamlit==1.38.0

# Base de datos
pymongo[srv]==4.8.0

# Datos y cálculo
pandas==2.2.2
numpy==1.26.4

# Gráficos
plotly==5.24.1

# Ingesta de mercado (Notebook 1)
# 1.5.1+ es OBLIGATORIO: incluye curl_cffi, que simula un navegador real
# para no ser bloqueado por la detección de bots de Yahoo Finance en IPs
# de datacenter como las de Streamlit Cloud (versiones 0.2.x antiguas
# fallan con 'Expecting value: line 1 column 1 (char 0)').
yfinance==1.5.1

# Clasificador SVC (Notebook 2)
scikit-learn==1.5.1

# Redes recurrentes: LSTM / BiLSTM / GRU / SimpleRNN (Notebooks 3-6)
# tensorflow-cpu es más liviano que 'tensorflow' completo — no hay GPU
# en Streamlit Community Cloud, así que no hace falta la variante GPU.
tensorflow-cpu==2.17.0
